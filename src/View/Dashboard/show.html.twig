{% extends 'layout.html.twig' %}

{% block content %}
<div class="container d-flex flex-column justify-content-around align-items-center h-100 mt-5">
    <h1 class="text-center mt-5 mb-5">🎶{{ track.title }} by {{ track.artist }}🎶 </h1>
    <div class="boxContainer invisible">
        <div class="box box1"></div>
        <div class="box box2"></div>
        <div class="box box3"></div>
        <div class="box box4"></div>
        <div class="box box5"></div>
    </div>
    <audio src="/uploads/tracks/{{track.path}}" class="audio mt-2" id="myAudio" controls></audio>
    <div class="container mt-5">
        <div class="row">
            <div class="col-12 col-md-6 py-2 d-flex flex-row justify-content-center justify-content-md-end">
                <a href="/dashboard" class="btn btn-tertiary">Back</a>
            </div>
            <div class="col-12 col-md-6 py-2 d-flex flex-row justify-content-center justify-content-md-start">
                <a href="/dashboard/update?id={{ track.id }}" class="btn btn-tertiary">Edit</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% block javascript %}
    <script src="/assets/js/dashboard_show.js"></script>
{% endblock %}
